import { Goal, Theme } from './types';

const themes: Theme[] = ['Carreira', 'Saúde', 'Finanças', 'Fé', 'Estudos', 'Outros'];

// Empty initial arrays - data will come from Supabase
export const users: any[] = [];

export const goals: Goal[] = [];
